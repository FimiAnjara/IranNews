document.addEventListener('DOMContentLoaded',function(){const mobileMenuToggle=document.querySelector('.mobile-menu-toggle');const mobileMenu=document.getElementById('mobileMenu');const mobileSearchToggle=document.querySelector('.mobile-search-toggle');const searchToggle=document.querySelector('.search-toggle');const searchBar=document.getElementById('searchBar');if(mobileMenuToggle&&mobileMenu){mobileMenuToggle.addEventListener('click',(e)=>{e.stopPropagation();mobileMenu.classList.toggle('open');mobileMenuToggle.setAttribute('aria-expanded',mobileMenu.classList.contains('open'))});const mobileLinks=mobileMenu.querySelectorAll('a');mobileLinks.forEach(link=>{link.addEventListener('click',()=>{mobileMenu.classList.remove('open');mobileMenuToggle.setAttribute('aria-expanded','false')})})}if(mobileSearchToggle&&searchBar){mobileSearchToggle.addEventListener('click',(e)=>{e.stopPropagation();const isOpen=searchBar.classList.toggle('is-open');searchBar.style.display=isOpen?'block':'none';mobileSearchToggle.setAttribute('aria-expanded',isOpen?'true':'false');if(isOpen){const searchInput=searchBar.querySelector('input[name="q"]');if(searchInput)searchInput.focus();}if(mobileMenu){mobileMenu.classList.remove('open');mobileMenuToggle.setAttribute('aria-expanded','false')}})}if(searchToggle&&searchBar){const searchInput=searchBar.querySelector('input[name="q"]');const closeSearch=()=>{searchBar.classList.remove('is-open');searchBar.style.display='none';searchToggle.setAttribute('aria-expanded','false')};searchToggle.addEventListener('click',(event)=>{event.stopPropagation();const isOpen=searchBar.classList.toggle('is-open');searchBar.style.display=isOpen?'block':'none';searchToggle.setAttribute('aria-expanded',isOpen?'true':'false');if(isOpen&&searchInput){searchInput.focus()}});document.addEventListener('click',(event)=>{if(!searchBar.contains(event.target)&&!searchToggle.contains(event.target)){closeSearch()}});document.addEventListener('keydown',(event)=>{if(event.key==='Escape'){closeSearch()}})}document.addEventListener('click',(event)=>{if(mobileMenu&&mobileMenuToggle){if(!mobileMenu.contains(event.target)&&!mobileMenuToggle.contains(event.target)){mobileMenu.classList.remove('open');mobileMenuToggle.setAttribute('aria-expanded','false')}}});const deleteLinks=document.querySelectorAll('.delete-link');deleteLinks.forEach(link=>{link.addEventListener('click',function(e){if(!confirm('Êtes-vous sûr?')){e.preventDefault()}})});const galleries=document.querySelectorAll('.article-gallery[data-gallery="slider"]');galleries.forEach((gallery)=>{const track=gallery.querySelector('.gallery-track');const items=track?Array.from(track.children):[];const prevButton=gallery.querySelector('.gallery-nav.prev');const nextButton=gallery.querySelector('.gallery-nav.next');if(!track||items.length<2){return}let index=0;items.forEach((item)=>{item.style.flex='0 0 100%'});const update=()=>{const slideWidth=gallery.clientWidth;track.style.transform=`translateX(-${index*slideWidth}px)`};prevButton?.addEventListener('click',(event)=>{event.preventDefault();index=(index-1+items.length)%items.length;update()});nextButton?.addEventListener('click',(event)=>{event.preventDefault();index=(index+1)%items.length;update()});update();window.addEventListener('resize',update)});const forms=document.querySelectorAll('form');forms.forEach(form=>{form.addEventListener('submit',function(e){const inputs=form.querySelectorAll('input[required], textarea[required]');let valid=true;inputs.forEach(input=>{if(!input.value.trim()){input.style.borderColor='red';valid=false}else{input.style.borderColor=''}});if(!valid){e.preventDefault();alert('Veuillez remplir tous les champs requis')}})})});
